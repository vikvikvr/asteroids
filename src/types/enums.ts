/* eslint-disable no-unused-vars */

export enum BulletPosition {
  Center,
  Left,
  Right
}

export enum AsteroidSize {
  Small,
  Medium,
  Large
}

export enum Temperature {
  Low,
  Normal,
  High
}
