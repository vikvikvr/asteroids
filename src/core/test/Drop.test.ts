// import Ship from './Ship';

// const coords = { x: 0, y: 0 };
// const world = { width: 500, height: 500 };
// let mock = jest.fn();
// var ship: Ship;

describe('drop', () => {
  it.todo('something');
});

export {};
